---
interface NavItem {
  label: string;
  href: string;
}

export interface Props {
  year?: number;
  statement?: string;
  homeHref?: string;
  navItems?: readonly NavItem[];
}

const {
  year = new Date().getFullYear(),
  statement = "一个安静的空间，留给工作、文字和下一步。",
  homeHref = "/",
  navItems = [
    { label: "作品", href: "/studio" },
    { label: "文章", href: "/journal" },
    { label: "关于", href: "/about" },
  ],
} = Astro.props;
---

<footer class="site-footer">
  <div class="site-shell site-footer__inner">
    <div class="site-footer__identity">
      <a class="site-footer__wordmark" href={homeHref} aria-label="alune 首页"
        >alune</a
      >
      <p>{statement}</p>
    </div>

    <nav class="site-footer__nav" aria-label="页脚导航">
      <ul>
        {
          navItems.map((item) => (
            <li>
              <a href={item.href}>{item.label}</a>
            </li>
          ))
        }
      </ul>
    </nav>

    <p class="site-footer__legal"><small>© {year} alune</small></p>
  </div>
</footer>
