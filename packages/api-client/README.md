# @alune/api-client

Reserved package for the generated frontend API client.

The intended next step is to generate TypeScript types, request functions, React Query hooks, and optional MSW mocks from FastAPI's `/openapi.json` using Orval.
